import { IReadResponse } from '../../utils/IReadResponse';
import { IAuthor } from '../../data/entities/IAuthor';
import { IBook } from '../../data/entities/IBook';
import { IMagazine } from '../../data/entities/IMagazine';
import { IRepository } from '../../common/IRepository';

// export interface ICsvRepository extends IRepository<> {
//   insertBooks(data: IReadResponse<IBook>): Promise<IBook[][]>;
//   insertAuthors(authors: IReadResponse<IAuthor>): Promise<IAuthor[][]>;
//   insertMagazines(magazines: IReadResponse<IMagazine>): Promise<IMagazine>;
// }
